# Build outputs
.gradle/
.kotlin/
build/
out/

# IDE
.idea/
*.iml

# Runtime data
data/

# Playwright MCP logs
.playwright-mcp/

# Playwright per-test artifacts (created when running specs from the
# repo root rather than under web-app/). Same content as
# web-app/test-results/ but landing here when the working dir was
# mistakenly the repo root.
test-results/

# Claude Code
.claude/

# Roku channel zip
roku-channel.zip

# iOS / Xcode (local developer config)
Developer.xcconfig
Branding.xcconfig
lifecycle/presubmit-personal-patterns.txt

# iOS / Xcode
*.p12
*.mobileprovision
*.cer
*.keystore
*.jks
xcuserdata/
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
*.xcodeproj/project.xcworkspace/
DerivedData/
*.ipa

# iOS generated protobuf/gRPC stubs
ios/MediaManager/MediaManager/Generated/

# Secrets
secrets/.env
secrets/roku-deploy.env
secrets/roku-deploy-dev.env
secrets/roku-deploy-prod.env
secrets/*.agent_visible_env
secrets/android-tv-keystore.jks
secrets/android-tv-signing.properties

# App Store demo setup — only example.env is checked in; the real
# .env carries the wizard-created admin password.
app_store_demo_setup/secrets/.env

# Vaadin generated (src/main)
src/main/bundles/
src/main/frontend/

# Angular SPA
web-app/node_modules/
web-app/.angular/
# Proto-derived TypeScript types — regenerated by `npm run proto:gen`
# (or transitively by the Gradle :generateWebProto task). Single source
# of truth lives in proto/; never commit the generated mirror.
web-app/src/app/proto-gen/

# Transcode buddy local config
transcode-buddy/buddy.properties
.DS_Store
