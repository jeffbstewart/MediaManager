# iOS device identifier for deployment.
# Find it with:
#   xcodebuild -project ios/MediaManager/MediaManager.xcodeproj \
#     -scheme MediaManager -showdestinations 2>&1 | grep "platform:iOS"
# Use the id value (e.g., 00008130-xxxx), not the CoreDevice UUID.
IOS_DEVICE_ID=YOUR-DEVICE-ID-HERE
