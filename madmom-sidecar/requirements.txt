# madmom + its pinned scientific deps. madmom 0.16.1 was cut in 2018
# and still references `np.float`, which numpy removed in 1.20. We
# pin numpy at the last 1.19 release and a matching scipy. Bumping
# any of these without testing will almost certainly surface a new
# deprecation-turned-removal at madmom import time.
madmom==0.16.1
numpy==1.19.5
scipy==1.5.4
cython==0.29.36

# gRPC server
grpcio==1.62.1
grpcio-tools==1.62.1

# Prometheus metrics + liveness probe
prometheus_client==0.20.0

# OpenTelemetry → Binnacle OTLP HTTP logs
opentelemetry-api==1.25.0
opentelemetry-sdk==1.25.0
opentelemetry-exporter-otlp-proto-http==1.25.0
